services:
  - type: static_site
    name: wingxtra-cloud-gcs
    env: static
    buildCommand: npm run render-build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
